# AGENTS.md

> 本文件是给 **AI 编码助手**（Codex CLI / GitHub Copilot / Claude / Cursor / 通义灵码 等）看的仓库说明书。
> 进入本仓库执行任何任务前，请先完整读完这份文件，并严格遵守。

## 1. 这是什么

个人 LeetCode 刷题仓库。语言 **Python**，目的 **后端开发求职准备**，开始日期 2026-09-03。

**核心定位：这是学习记录，不是工程代码。** 仓库刻意保留错误版本作为反面教材，
衡量标准是「思路是否想清楚了」，不是「代码是否最短最优」。

## 2. 目录结构

```
arrays/      数组
dp/          动态规划
hash_table/  哈希表
linked_list/ 链表
math/        数学
```

每道题一个文件夹：`专题/NNN-英文题名/solution_vN.py`

- 题号补零到三位：`001-two-sum`、`026-remove-duplicates-from-sorted-array`、`283-move-zeroes`
- 题名用 LeetCode 官方英文名，全小写、连字符分词
- 新专题直接建新目录，命名同风格（小写、下划线）

## 3. 文件头格式（硬性要求）

每个 `.py` 文件开头必须是这四行注释，一行不能少：

```python
# LeetCode 26. 删除有序数组中的重复项（简单）
# 套路：相邻比较 + len(set) 交差 —— 反面教材，运行时 IndexError，且从未真正搬移元素
# 2026-09-12
# 我的解答（错误示范，保留以记录 bug）
```

依次是：**题号 + 中文题名 +（难度）** / **套路 + 复杂度 + 问题标注** / **日期** / **补充说明**。

- 难度只写三种：`（简单）`（中等）（困难）
- 复杂度必须写，如 `O(n)`、`O(n log n)`、`O(1) 空间`
- 补充行说明这份文件的性质：「错误示范」「正确解」「优化版」「一行流」等

## 4. 多版本约定

同一题按**尝试顺序**存 `solution_v1.py`、`solution_v2.py`、`solution_v3.py`……

- **v1 通常是暴力解或错误示范**，这是有意为之，**不要删除、不要"顺手修正"**
- 每个版本独立成文件，不要合并
- 版本之间是"我的思考过程"：`v1 暴力 → v2 改进 → v3 最优`
- 若某版是错的，文件头补充行必须明说（见上例），并在踩坑记录里写清为什么错

## 5. 测试要求

文件末尾必须有 `if __name__ == "__main__":`，用 **assert** 做本地验证，
保证 `python solution_vN.py` 一条命令直接跑通。

- 至少覆盖：官方示例用例、边界（空输入 / 单元素 / 全相同 / 负数）
- 反面教材文件要用 `try/except` 把「预期的报错」捕获并打印，证明它确实会崩
- 不要用 pytest / unittest，保持零依赖，assert 就够了

## 6. 踩坑记录（本仓库最有价值的部分）

`if __name__` 之后，文件最末尾写「踩坑记录」，格式：

```python
# 踩坑记录
# 1. 【越界】for i in range(len(nums)) 里访问 nums[i + 1]，i = len(nums)-1 时必然越界。
#    只要写「和下一个元素比」的循环，上界就要写成 len(nums) - 1。
# 2. 【条件恒假】数组非严格递增，nums[i] > nums[i+1] 永远不会成立，循环体一次都没执行。
# 3. 正确写法见 solution_v2.py：快慢双指针，一趟扫描，O(n) 时间 / O(1) 空间。
```

规则：
- 每条用【错误类型】开头，如【越界】【条件恒假】【空输入】【类型错误】【超时】
- 写清 **错在哪 → 为什么错 → 怎么改**
- 最后一条指向正确版本（如果有）
- **只记真实踩过的坑**，不要编造、不要凑数

## 7. 完成一题后必须更新 README

两处都要改，缺一不可：

1. **进度总览**：对应类别 +1，合计 +1
2. **题目清单**：加一行 `| 26 | 删除有序数组中的重复项 | 简单 | 数组 | 相邻比较越界 WA / 快慢双指针 O(n) | 2026-09-12 | ✅ |`

## 8. 提交信息格式

沿用现有风格：

```
add LC26 删除有序数组中的重复项：越界+只交数量的 WA 反面教材 / 快慢双指针 O(n)，含踩坑记录
```

即：`add LC<题号> <中文题名>：<v1 套路> / <v2 套路>，含踩坑记录`

## 9. 硬性禁止事项

- ❌ **不要删除或"优化"已有的 solution_vN.py**，尤其是标着错误示范的
- ❌ **不要引入第三方库**（numpy / pytest / collections_extended 等一律不用）
- ❌ **不要直接改动历史文件的头注释日期**，那是完成日期，不是修改日期
- ❌ 不要用英文写注释和踩坑记录，**全程中文**
- ❌ 不要动 `.git` 目录
- ✅ **推送授权**：用户说「整理到仓库 / 整理进仓库 / 同步 / 推上去」即视为已授权，**直接 commit + push 一气呵成，无需再询问确认**（push 前提醒用户开 Watt Toolkit 的 GitHub 加速即可）
- ❌ 不要为了"整洁"重排目录、重命名文件夹

## 10. 代码风格

- 类名固定 `Solution`，方法签名照抄 LeetCode 官方给定形式
- 保留 `from typing import List` 风格；新文件也可直接用内置泛型 `list[int]`，**同一文件内保持一致**
- 函数写 docstring 说明参数与返回值
- 变量命名清晰，允许适当的中间变量（可读性 > 极简）
- 行内注释解释 **为什么**，而不是复述代码在做什么

## 11. 教学原则（最重要，务必遵守）

使用者是在校学生，用这个仓库**练思路**。因此：

1. **不要一上来就给最优解。** 先问「你打算怎么做」或给方向性提示。
2. **报错了先讲为什么。** 指出问题所在的思路漏洞，让他自己动手改；
   只有在明确要求「直接给代码」时才写完整实现。
3. **鼓励先写暴力解。** 暴力解也值得单独存成 `solution_v1.py`，再逐步优化。
4. **复杂度分析要讲。** 每份代码都要能说清时间和空间复杂度，写进文件头。
5. **同类题要提示迁移。** 比如做到哈希表题，可以点一句「这题和 LC1 两数之和是一个套路」。
6. **不要替他做题。** 目标是让他下次遇到同类题能自己做出来，不是让仓库多一个 ✅。

---

_最后更新：2026-09-22_
